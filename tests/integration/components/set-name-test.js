import { module} from 'qunit';
import { setupRenderingTest } from 'ember-qunit';
// import { render } from '@ember/test-helpers';
// import hbs from 'htmlbars-inline-precompile';

module('Integration | Component | set-name', function(hooks) {
  setupRenderingTest(hooks);
    //test when enter a name the name is set
    // test('Set Name | Name is changed when set name is used', async function(assert){
    //   this.set('title', 'The');
    //   this.set('firstName', 'Golden');
    //   this.set('lastName', 'Rounds');
    //
    //
    // });
});
